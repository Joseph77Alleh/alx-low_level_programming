#include <stdio.h>

/**
 * main - prints its name folowed by a new line.
 * @argc: parameter
 * @argv: array
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

